spring:
  application:
    name: auditlog-service

  config:
    import: optional:configserver:http://localhost:8888

  cloud:
    config:
      fail-fast: true

  kafka:
    consumer:
      group-id: audit-log-group